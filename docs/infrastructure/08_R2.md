# 08 — Cloudflare R2

## Resumen

Object storage offsite para dumps PostgreSQL de producción (y storage staging registrado en Coolify).

## Estado

Bucket prod usable · 4 dumps presentes · sync rclone operativo.

**Última verificación UTC:** 2026-08-05T10:00Z

## Inventario

| Item | Valor |
|---|---|
| Bucket prod | `argos-it-production-backups` |
| Coolify storage UUID | `qzrnhrzylrp6ngup2ahfalox` |
| Bucket staging (Coolify) | `argos-it-staging-backups` (`ld0jk183xv81x4wyn3gm2f9s`) |
| Prefijo dumps prod | `data/coolify/backups/databases/root-team-0/argos-it-production-db-iw42qpqc1w1umsddrl9fwpi9/` |
| Cliente host | rclone + conf `/root/argos-prod-ops/keys/rclone-r2.conf` |

## Endpoint / política

Endpoint exacto Account ID / política IAM: **NO VERIFICADO** en esta pasada (no leer secretos).  
Diseño: API S3-compatible Cloudflare R2.

## Flujo

1. Coolify genera `.dmp` local.
2. `r2-offsite-sync.sh` (cron 15 0 * * *) sube vía rclone.
3. Monitor cuenta objetos y edad.

## Monitorización

`monitor-production.sh` → `offsite_dump_count`, `offsite_age_h`, `coolify_r2_count`, `offsite_rclone_ok`.

## Retención

Config Coolify S3 30/30 · objetos actuales 4 · purge automático R2 lifecycle: **NO VERIFICADO**.

## Dependencias

Token R2 · DNS Cloudflare API · red saliente VPS.

## Riesgos

Fallo histórico `mc` IPv6 · token rotación no documentada en vault ops formal.

## Rollback

N/A objetos; re-sync desde dumps locales.

## Observaciones

Access / Tunnel / Workers Cloudflare: **no implementados** (solo R2 en uso operativo).

**Última verificación UTC:** 2026-08-05T10:00Z
